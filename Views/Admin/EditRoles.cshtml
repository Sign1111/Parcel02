@model EditRolesViewModel

<h2>Edit Roles for @Model.UserName</h2>

<form asp-action="EditRoles" method="post">
    <input type="hidden" asp-for="UserId" />

    <div class="form-group">
        <label>Roles</label>
        <select asp-for="SelectedRoles" class="form-control" multiple="multiple">
            @foreach (var role in Model.AssignedRoles)
            {
                <option value="@role.Value" selected="@(role.Selected ? "selected" : null)">@role.Text</option>
            }
        </select>
    </div>

    <button type="submit" class="btn btn-primary mt-3">Save</button>
</form>
